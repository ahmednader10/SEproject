<ul><% if current_user.friendships.size !=0 %>

<% @users.each do |user| %>

<% if user == current_user %>

<% else %>





<% for friendship in user.friendships %>
<li>
	<% if friendship.status == false   %>
  
    <%= h friendship.requester %>
    (<%= link_to "cancel", friendship, :method => :delete %>)
    <%else%>
 <em> no requested friends </em>
  <% end %> 
  </li>
  <% end %> 
  <% end %> 
  <% end %>  
  <%else%>
  <em> no requested friends </em>
  <% end %>
  </ul>

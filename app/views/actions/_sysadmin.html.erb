<!DOCTYPE html>
<html lang="en">
	<head>
	<title>Bootstrap Example</title>
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css">
		<%= stylesheet_link_tag 'style' %>
		<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
		<script src="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/js/bootstrap.min.js"></script>
		<script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-select/1.6.5/js/bootstrap-select.min.js"></script>
		<link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css">
		<style type="text/css">
		ul{
			list-style: none;
		}
		li{
			float: left;
			margin-right: 30px;
		}
		i{
			width:155px;
			height: 155px;
			border-radius: 50%;
			border: 2px solid #005CE6;
			font-size: 3.5em;
			text-align: center;
			padding: 15px;
		}

		p{
			text-align: center;
		}
		</style>
	</head>
	<body style="background: #EEE;">
			<%= render "/shared/navbar_sys_admin"%>
		<div style="margin-top:5px; ">
			<div style=" position: absolute; top:20%; left:20%;">	
				<ul>
					<li><a><i class="glyphicon glyphicon-folder-open"></i><p style="position:absolute; top:100px; left:65px;"> <%= Forum.where("created_at >= ?", Time.zone.now.beginning_of_day).count %> created today</p></a></li>
					<li><a><i class="glyphicon glyphicon-user"></i><p style="position:absolute; top:100px; left:247px;"><%= User.where("created_at >= ?", Time.zone.now.beginning_of_day).count %> signed up today</p ></a></li>
					<li><a><i class="glyphicon glyphicon-ok"></i><p style="position:absolute; top:95px; left:424px;"><%= Membership.where("created_at >= ?", Time.zone.now.beginning_of_day).count %> new Memberships <br> today</p></a></li>
					<li><a><i class="glyphicon glyphicon-heart"></i><p style="position:absolute; top:95px; left:608px;"><%= Idea.where("created_at >= ?", Time.zone.now.beginning_of_day).count %> Ideas were posted <br> today</p></a>
					</li>
				</ul>
			</div>
			<div class="table-responsive" style="position: absolute;  top:50%; left:10%;">
			
			
				<table class="table-striped" style="width:1000px;">
				<% @actions.reverse_each do |action| %>
					<tr>
						<td style="text-align:center; width:20%;">
							<% if action.hidden == false %> 
							<%= link_to 'hide', 'syslog/hide/' + action.id.to_s, method: :put %>
							<% else %> 
							<%= link_to 'unhide', 'syslog/unhide/' + action.id.to_s, method: :put %>
							<% end %> 
						</td>
						<td style="text-align:center; width:60%;">
							<%= action.info + ' (' + action.created_at.to_s + ')' %> 
						</td>
						<td style="text-align:center;width:20%;">
							<% user = User.where(email: action.user_email).first %>
							<% if user != nil %>
							<%= link_to ' ' + User.where(email: action.user_email).first.username, 'users/' + User.where(email: action.user_email).first.id.to_s %>
							<% else %>
							<%= action.user_email %>
						<% end %>
					</td>
				</tr>
				<% end %>
				</table>
			</div>
			</div>
	</body>
</html>

